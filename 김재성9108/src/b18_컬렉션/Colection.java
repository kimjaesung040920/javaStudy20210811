package b18_컬렉션;

import java.awt.List;
import java.util.Collection;

/**
 * 
 * 컬렉션(Collection)
 * java에서의 데이터 관리를 위한 라이브러리. 
 * 
 * list : 순서가 있음(index를 거친다음 찾는다)
 * set : 순서가 없음(원하는 값을 바로 찾는다)[데이터가 많으면 그만큼 뒤에있는 값이면 꺼내는값도 많아짐]
 * map : key값과 value가 있음 / Map의 동작 방식은 List, key값은 Set
 * select(조회) : 데이터가 많을때 list, 데이터가 적을때 set
 * insert(추가) : set이 빠르다.
 * update(수정) : List
 * delete(삭제) : List
 * ArrayList : 
 * Vector : 값이 하나 들어오면 닫힌다.
 * 
 * 
 */


public class Colection {
	Collection<String> c ;
	
}
